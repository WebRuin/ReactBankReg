import React from "react";

import styles from "./styles/header.scss";

export default class Header extends React.Component {
  render() {

    return (
      <header>
        React Bank Registery
      </header>
    );
  }
}
